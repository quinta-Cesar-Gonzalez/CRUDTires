service: crud-tires

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1  # Cambiado a us-east-1 para estar en la misma región que RDS
  stage: ${opt:stage, 'dev'}
  environment:
    MYSQL_URI: ${env:MYSQL_URI}
  # Lambda configuration optimizada
  memorySize: 512
  timeout: 30

functions:
  api:
    handler: index.handler
    description: CRUD API for Tires Catalog
    events:
      # Catch-all proxy para todas las rutas
      - http:
          path: /
          method: ANY
          cors:
            origin: '*'
            headers:
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
      - http:
          path: /{proxy+}
          method: ANY
          cors:
            origin: '*'
            headers:
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
  
  test:
    handler: test-lambda.handler
    description: Test database connection and environment
    events:
      - http:
          path: /test
          method: GET
          cors: true

# Package configuration
package:
  excludeDevDependencies: true
  patterns:
    - '!.git/**'
    - '!.env*'
    - '!node_modules.zip'
    - '!lambda-deployment.zip'
    - '!lambda-package/**'
    - '!.github/**'
    - '!*.md'
    - 'public/**'
    - 'index.js'
    - 'database.js'
    - 'test-lambda.js'
    - 'package.json'

plugins:
  - serverless-offline

custom:
  serverless-offline:
    httpPort: 3000
